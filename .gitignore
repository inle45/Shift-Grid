node_modules
dist
# icons are generated at build time — ne pas commiter les PNG
# (décommente si tu veux les versionner)
# public/icons/*.png
.env
.env.local
*.log

# Clé de signature Android — NE JAMAIS commiter
*.keystore
*.jks

# Android build outputs
android/app/release/
android/.gradle/
