[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_BASE = "/"

# SPA routing — renvoie toujours vers index.html
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
